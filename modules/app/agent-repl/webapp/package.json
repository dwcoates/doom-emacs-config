{
  "name": "claude-repl-webapp",
  "version": "0.1.0",
  "private": true,
  "description": "Layer-2 WebSocket SPA client for claude-repld",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.1"
  }
}
