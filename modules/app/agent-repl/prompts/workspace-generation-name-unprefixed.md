<!-- used by: worktree.el (agent-repl--workspace-generation-prompt, when no workspace branch prefix is configured); placeholders: none -->
Generate the `name' field as <short-slug> (lowercase, hyphenated, 3 words max) based on the DESCRIPTION above.
