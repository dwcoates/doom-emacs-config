## JUDGE
- teams.dba.public.skills.creating-a-mysql-schema-migration	77.38	
- teams.gns-devs.public.skills.project-builder	72.27	Turn an idea into a delivered project. Phase 1 interactively builds a comprehensive, ground-truth implementation plan — a Mermaid node-graph DAG sequenced to eliminate rework, with frozen architecture contracts, business-logic decisions, and effort-aware parallelism. Phase 2 executes the DAG by spawning GNS agents wave by wave, reconciling their outputs, and assembling commits across any number of repos. Use when the user says build this project, plan and build, implement this idea end to end, orchestrate agents to ship a feature, or wants a planned multi-agent build across repos.
- user.dodge.skills.gns-sockets	62.07	Manage the gns-sockets GNS plugin lifecycle and drive Slack-thread subscriptions and posting. Use `init` once after pulling, then `status` / `logs` / `build` to manage, or `subscribe` / `unsubscribe` / `close` / `mode` / `list` / `tail` / `post` / `git` to interact. Event delivery is push-based via Claude Code hooks installed automatically on subscribe; the agent does not poll. Also hosts the reaction initiator (folded-in agent-slackbot) a watched emoji shortcut in a watched channel spins up a workspace that subscribes to the originating thread. **Prefer `close` over `unsubscribe` when the user phrases the request as "close" — same engine call, friendlier name.**
- teams.core-chess.skills.gns-sockets	59.21	Manage the gns-sockets GNS plugin lifecycle and drive Slack-thread subscriptions and posting. Use `init` once after pulling, then `status` / `logs` / `build` to manage, or `subscribe` / `unsubscribe` / `close` / `mode` / `list` / `tail` / `post` / `git` to interact. Event delivery is push-based via Claude Code hooks installed automatically on subscribe; the agent does not poll. Also hosts the reaction initiator (folded-in agent-slackbot) a watched emoji shortcut in a watched channel spins up a workspace that subscribes to the originating thread. **Prefer `close` over `unsubscribe` when the user phrases the request as "close" — same engine call, friendlier name.**
- teams.train.public.skills.write-rfc	56.36	Draft Chess.com engineering RFCs and design docs that lead with system and sequence diagrams, follow our best in-house examples, and record decisions as options + comparison table + recommendation. Use when the user asks to write, draft, scaffold, or review an RFC, design doc, or technical proposal — or asks "should this be an RFC or an ADR?".
- teams.dba.public.skills.requesting-a-new-redis-instance	50.76	
- teams.architecture.public.skills.building-blocks	48.48	Use when asked about ChessCom IDL building blocks, implementation patterns, golden code examples, or how to correctly implement a reusable component in a specific runtime.
- teams.gns-devs.public.skills.architecture-diagram	46.40	Create Chess.com architecture and system diagrams using Mermaid, following org conventions, and bank them to GNS keyspace. Use when the user says system diagram, architecture diagram, draw a system, map a service, visualize infrastructure, create a diagram, or asks to diagram how something works.
- gns.skills.gns-building-blocks	46.18	A catalog of reusable, self-learning GNS automation loops you can stand up from existing primitives — every one a specialization of the dreaming-work-queue pattern. Use when you want to build an agentic loop that picks up work the moment it appears, learns overnight, and is steered by comments. Covers self-healing runbooks, doc-drift sentinels, onboarding concierges, PR-quality flywheels, decision ledgers, signal meshes, prompt evolution, command-center apps, space mappers, knowledge convergence, and a set of Neural-Network-Zoo motifs (adversarial generator/critic pairs, knowledge autoencoders, residual refiners, associative recall, controller + external memory, reservoirs, convolutional scanners). Trigger when the user says building block, self-learning loop, dreaming-work-queue variant, or names one of these blocks.
- teams.architecture.skills.rfc-rake-and-take	43.53	
EXIT=1
